import ProjectWorkspace from "../Components/ProjectWorkspace"

// List View (spec §11): tabela hierárquica com edição inline de status/prioridade.
// Mesmo componente do Board.page — a view sai do pathname.
export default ProjectWorkspace
