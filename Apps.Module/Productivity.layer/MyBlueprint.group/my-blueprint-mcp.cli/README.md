# My Blueprint MCP

Após instalar, configure o cliente MCP para executar `my-blueprint-mcp serve`.
Use `MB_SESSION_PROVIDER=codex`, `claude` ou `other`; no OpenCode com GLM use
`other` e informe o modelo GLM real em `MB_SESSION_MODEL`.
