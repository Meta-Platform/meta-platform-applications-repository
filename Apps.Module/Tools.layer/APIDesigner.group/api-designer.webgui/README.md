# api-designer.webgui

Front-end (interface gráfica) do **API Designer**. É um dos pacotes do grupo
`APIDesigner.group`, consumido pela composição `api-designer.webapp`
(e pela variante `api-designer.desktopapp`) sobre um `@@/server-service`.
Não é executado isoladamente: sobe montado pela aplicação do grupo.
