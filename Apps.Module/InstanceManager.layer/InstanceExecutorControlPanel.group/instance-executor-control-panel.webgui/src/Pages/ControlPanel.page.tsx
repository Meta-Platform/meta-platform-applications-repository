import * as React from "react"

import PanelShell from "../Containers/PanelShell/PanelShell.container"

// Instance Executor — página única do painel; toda a navegação (seções, abas e
// instância selecionada) acontece dentro do shell e é endereçável pela URL.
const ControlPanelPage = () => <PanelShell/>

export default ControlPanelPage
